import { observer } from 'mobx-react';


export type BlockProps = {
};

export default observer(function Block() {
    return (
        <div 
            className="ReactTimeline__Block"
        >
        </div>
    )
});
